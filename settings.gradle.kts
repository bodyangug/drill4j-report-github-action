rootProject.name = "drill4j-report-github-action"

pluginManagement {
    plugins {
        kotlin("jvm") version "1.5.31"
    }

    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
}
